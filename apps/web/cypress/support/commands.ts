// Custom Cypress commands
// Example: cy.login('user@example.com', 'password')

export {}

declare global {
  namespace Cypress {
    interface Chainable {
      // Add custom command types here
    }
  }
}
